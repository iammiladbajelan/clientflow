import { Landing } from "@/components/landing";

export const metadata = {
  title: "ClientFlow — Run your freelance business like a pro",
  description:
    "Clients, projects, tasks and revenue in one clean workspace. Built for freelancers and small agencies.",
};

export default function HomePage() {
  return <Landing />;
}
